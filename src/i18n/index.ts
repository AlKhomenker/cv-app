/**
 * The language the interface is written in: the provider, the hook that reads
 * it, and nothing else. The words themselves live in `@/content`.
 */
export { LocaleProvider } from "./LocaleProvider";
export { useLocale } from "./useLocale";
export type { LocaleValue } from "./LocaleContext";
